#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    char s[100];
    scanf("%d",s);
    int length;
    while(length!='\0'){
        length++;
    }
    return 0;
}